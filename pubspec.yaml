name: build_variant
description: A new Flutter package project.
version: 0.0.2
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  yaml: ^3.1.0
  dart_style: ^2.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
